import React from 'react'

const page = () => {
  return (
    <div>
      this is the admin login page
    </div>
  )
}

export default page
export const metadata = {
  title: 'Login hai bhai ',
}


// so we can generate the metadata for the page and also we can use the layout for the page
