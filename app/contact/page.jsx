import React from 'react'

const page = () => {
  return (
    <div>
      this is the contact page 
    </div>
  )
}

export const metadata = {
    title: "contact page",
    description: "this is the contact page of the website",
    };

export default page

// so save the meta data in the page itself and then use it in the layout.js file
// cleint side ke lie we use the link tag which helps in linking the page 
